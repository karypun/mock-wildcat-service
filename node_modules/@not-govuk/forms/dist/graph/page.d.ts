import { Graph } from './graph';
import { Node } from './node';
import { PathItem } from './path';
export declare class PageItem implements PathItem {
    tag: string;
    active: boolean;
    constructor(active: boolean);
}
export declare class PageNode extends Node {
    readonly tag: string;
    contents: Graph;
    active: boolean;
    fields?: string[];
    constructor(contents: Graph);
    toPathItem(): PathItem;
    depopulate(): void;
    populateFromValues(values: any): void;
    populateFromNext(next: any): void;
    toArray(): Node[];
    traverse(values: any): Graph;
}
export declare const isPageNode: (v: any) => v is PageNode;
export declare const isPageItem: (v: any) => v is PageItem;
