import { FC } from 'react';
import { StandardProps } from '@not-govuk/component-helpers';
export { withField, withForm, withControl } from './hocs';
export { Page } from './page';
export { Fork } from './fork';
export { after, alpha, alphanumeric, before, date, email, exactLength, future, integer, maximum, maxLength, maxWords, minimum, minLength, minWords, mobileNumber, numeric, past, postalCode, range, required, url, validator } from './validators';
declare type Method = 'get' | 'post';
export declare type FormProps<T> = StandardProps & {
    action: string;
    debug?: boolean;
    initialValues?: T;
    method: Method;
    validate?: (values: T) => T;
};
export declare const Form: FC<FormProps<any>>;
export default Form;
export type { RawField } from './hocs';
