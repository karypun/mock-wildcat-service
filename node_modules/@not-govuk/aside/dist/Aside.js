import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/Aside.scss';
export const Aside = ({ children, classBlock, classModifiers, className, ...attrs }) => {
    const classes = classBuilder('not-govuk-aside', classBlock, classModifiers, className);
    return (h("aside", Object.assign({}, attrs, { className: classes() }), children));
};
export default Aside;
