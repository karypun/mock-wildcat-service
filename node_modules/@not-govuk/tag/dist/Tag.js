import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/Tag.scss';
export const Tag = ({ children, classBlock, classModifiers, className, text, ...attrs }) => {
    const classes = classBuilder('govuk-tag', classBlock, classModifiers, className);
    return (h("strong", Object.assign({}, attrs, { className: classes() }), text || children));
};
export default Tag;
