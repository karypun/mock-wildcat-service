import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/SimpleTable.scss';
export const SimpleTable = ({ caption, classBlock, classModifiers, className, data, headings, keys, ...attrs }) => {
    const classes = classBuilder('penultimate-simple-table', classBlock, classModifiers, className);
    return (h("table", Object.assign({}, attrs, { className: classes() }),
        caption && (h("caption", { className: classes('caption') }, caption)),
        h("thead", { className: classes('head') },
            h("tr", { className: classes('row') }, keys.map((k, i) => (h("th", { key: i, scope: "col", className: classes('header') }, headings[k]))))),
        h("tbody", { className: classes('body') }, data.map((e, io) => (h("tr", { key: io, className: classes('row') }, keys.map((k, i) => (i === 0
            ? (h("th", { key: i, scope: "row", className: classes('header') }, e[k]))
            : (h("td", { key: i, className: classes('cell') }, e[k]))))))))));
};
export default SimpleTable;
