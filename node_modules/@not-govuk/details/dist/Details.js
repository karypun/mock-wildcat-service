import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/Details.scss';
export const Details = ({ children, classBlock, classModifiers, className, summary, ...attrs }) => {
    const classes = classBuilder('govuk-details', classBlock, classModifiers, className);
    return (h("details", Object.assign({}, attrs, { className: classes() }),
        h("summary", { className: classes('summary') }, summary),
        h("div", { className: classes('text') }, children)));
};
export default Details;
