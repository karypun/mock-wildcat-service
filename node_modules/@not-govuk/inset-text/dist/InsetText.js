import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/InsetText.scss';
export const InsetText = ({ children, classBlock, classModifiers, className, ...attrs }) => {
    const classes = classBuilder('govuk-inset-text', classBlock, classModifiers, className);
    return (h("div", Object.assign({}, attrs, { className: classes() }), children));
};
export default InsetText;
