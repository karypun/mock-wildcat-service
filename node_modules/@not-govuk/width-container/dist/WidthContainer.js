import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/WidthContainer.scss';
export const WidthContainer = ({ children, classBlock, classModifiers, className, maxWidth, ...attrs }) => {
    const classes = classBuilder('govuk-width-container', classBlock, classModifiers, className);
    const style = (maxWidth === undefined
        ? undefined
        : {
            maxWidth: (maxWidth === -1
                ? 'none'
                : `${maxWidth}px`)
        });
    return (h("div", Object.assign({}, attrs, { className: classes(), style: style }), children));
};
export default WidthContainer;
