import { createElement as h } from 'react';
import { useHistory } from 'react-router-dom';
import { classBuilder } from '@not-govuk/component-helpers';
import { A } from '@not-govuk/link';
import '../assets/BackLink.scss';
export const BackLink = ({ children, classBlock, classModifiers, className, href, text = 'Back', ...attrs }) => {
    const defaultClassBlock = 'govuk-back-link';
    const classes = classBuilder(defaultClassBlock, classBlock, classModifiers, className);
    const history = useHistory();
    return href ? (h(A, Object.assign({}, attrs, { classBlock: classBlock || defaultClassBlock, classModifiers: classModifiers, className: className, href: href }), text)) : (h("a", Object.assign({}, attrs, { className: classes(), href: "#", onClick: history.goBack }), text));
};
export default BackLink;
