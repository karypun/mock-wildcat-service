import { createElement as h } from 'react';
import { AnchorList } from '@not-govuk/anchor-list';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/NavigationMenu.scss';
export const NavigationMenu = ({ children, classBlock, classModifiers, className, items, ...attrs }) => {
    const classes = classBuilder('not-govuk-navigation-menu', classBlock, classModifiers, className);
    return (h("nav", Object.assign({}, attrs, { className: classes() }),
        h(AnchorList, { classBlock: classes('list'), items: items })));
};
export default NavigationMenu;
