import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import { Tag } from '@not-govuk/tag';
import '../assets/PhaseBanner.scss';
export const PhaseBanner = ({ children, classBlock, classModifiers, className, phase, ...attrs }) => {
    const classes = classBuilder('govuk-phase-banner', classBlock, classModifiers, className);
    return (h("div", Object.assign({}, attrs, { className: classes() }),
        h("p", { className: classes('content') },
            h(Tag, { className: classes('tag') }, phase),
            h("span", { className: classes('text') }, children))));
};
export default PhaseBanner;
