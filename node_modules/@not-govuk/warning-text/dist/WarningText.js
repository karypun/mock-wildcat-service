import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/WarningText.scss';
export const WarningText = ({ children, classBlock, classModifiers, className, assistiveText = 'Warning', ...attrs }) => {
    const classes = classBuilder('govuk-warning-text', classBlock, classModifiers, className);
    return (h("div", Object.assign({}, attrs, { className: classes() }),
        h("span", { className: classes('icon'), "aria-hidden": "true" }, "!"),
        h("strong", { className: classes('text') },
            h("span", { className: classes('assistive') }, assistiveText),
            children)));
};
export default WarningText;
