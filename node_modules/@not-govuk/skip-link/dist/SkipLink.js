import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/SkipLink.scss';
export const SkipLink = ({ children = 'Skip to main content', classBlock, classModifiers, className, for: forProp, ...attrs }) => {
    const classes = classBuilder('govuk-skip-link', classBlock, classModifiers, className);
    const href = `#${forProp}`;
    return (h("a", Object.assign({}, attrs, { className: classes(), href: href }), children));
};
export default SkipLink;
