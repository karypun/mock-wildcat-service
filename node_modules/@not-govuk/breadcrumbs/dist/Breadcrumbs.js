import { createElement as h } from 'react';
import { AnchorList } from '@not-govuk/anchor-list';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/Breadcrumbs.scss';
export const Breadcrumbs = ({ children, classBlock, classModifiers, className, items, ...attrs }) => {
    const classes = classBuilder('govuk-breadcrumbs', classBlock, classModifiers, className);
    return (h("div", Object.assign({}, attrs, { className: classes() }),
        h(AnchorList, { as: "ol", items: items, classBlock: classes('list') })));
};
export default Breadcrumbs;
