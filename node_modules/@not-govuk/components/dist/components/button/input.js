import { createElement as h } from 'react';
const Input = props => (h("input", { id: props.id, className: props.className, type: props.type, value: props.text, disabled: props.disabled, onClick: props.onClick }));
export default Input;
