import { createElement as h } from 'react';
import { A } from '../';
const Anchor = props => (h(A, { classBlock: props.className, "data-module": "govuk-button", draggable: false, href: props.href, id: props.id, role: "button" }, props.children || props.text));
export default Anchor;
