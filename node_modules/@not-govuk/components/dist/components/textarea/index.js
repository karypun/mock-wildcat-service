import * as React from 'react';
import FormField from '../form-field';
;
export const Textarea = props => React.createElement(FormField, {
    autoComplete: props.autoComplete,
    className: props.className,
    defaultValue: props.defaultValue,
    disabled: props.disabled,
    error: props.error,
    hint: props.hint,
    id: props.id,
    label: props.label,
    name: props.name,
    onBlur: props.onBlur,
    onChange: props.onChange,
    rows: props.rows,
    spellCheck: props.spellCheck,
    type: 'textarea',
    value: props.value
});
Textarea.defaultProps = {
    autoComplete: null,
    className: null,
    defaultValue: undefined,
    disabled: false,
    error: null,
    hint: null,
    id: null,
    onBlur: null,
    onChange: null,
    rows: null,
    spellCheck: null,
    value: undefined
};
export default Textarea;
