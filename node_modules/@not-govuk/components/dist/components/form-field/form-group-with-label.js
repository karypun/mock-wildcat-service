import { createElement as h } from 'react';
import FormGroup from './form-group';
import Hint from './hint';
import Label from './label';
import ErrorMessage from './error-message';
const FormGroupWithLabel = props => (h(FormGroup, { id: props.id, className: props.className },
    h(Label, { htmlFor: props.fieldId }, props.label),
    props.hint && h(Hint, { id: `${props.id}-hint` }, props.hint),
    props.error && h(ErrorMessage, { id: `${props.id}-error` }, props.error),
    props.children));
export default FormGroupWithLabel;
