import { createElement as h } from 'react';
import CheckboxesAndRadios from './checkboxes-and-radios';
import Hint from './hint';
import Label from './label';
import { bem } from '../../helpers';
const Checkboxes = props => (h(CheckboxesAndRadios, { id: props.id, className: props.className, label: props.label, hint: props.hint, error: props.error, inline: props.inline, small: props.small },
    h("div", { className: bem('govuk-checkboxes', props.small && 'small', props.inline && 'inline') }, props.options.map((v, i) => {
        const id = `${props.id}-checkbox-${i}`;
        return (h("div", { className: "govuk-checkboxes__item", key: i },
            h("input", { id: id, className: "govuk-checkboxes__input", name: props.name, type: "checkbox", value: v.value, checked: v.selected, disabled: props.disabled || v.disabled, onBlur: props.onBlur, onChange: props.onChange }),
            h(Label, { htmlFor: id, className: "govuk-checkboxes__label" }, v.label),
            v.hint && h(Hint, { id: `${id}-hint`, className: "govuk-checkboxes__hint" }, v.hint)));
    }))));
export default Checkboxes;
