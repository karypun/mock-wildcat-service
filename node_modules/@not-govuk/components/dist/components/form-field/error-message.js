import { createElement as h } from 'react';
const ErrorMessage = props => (h("span", { id: props.id, className: "govuk-error-message" },
    h("span", { className: "invisible" }, "Error:"),
    " ",
    props.children));
export default ErrorMessage;
