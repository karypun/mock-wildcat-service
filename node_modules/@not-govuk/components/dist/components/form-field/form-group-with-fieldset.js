import { createElement as h } from 'react';
import FormGroup from './form-group';
import Hint from './hint';
import ErrorMessage from './error-message';
const FormGroupWithFieldset = props => {
    const hintId = props.hint && `${props.id}-hint`;
    return (h(FormGroup, { id: props.id, className: props.className },
        h("fieldset", { "aria-describedby": hintId, className: "govuk-fieldset", role: props.role },
            h("legend", { className: "govuk-fieldset__legend" }, props.legend),
            props.hint && h(Hint, { id: hintId }, props.hint),
            props.error && h(ErrorMessage, { id: `${props.id}-error` }, props.error),
            props.children)));
};
export default FormGroupWithFieldset;
