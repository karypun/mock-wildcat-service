import { createElement as h } from 'react';
import { className } from '../../helpers';
const Label = props => (h("label", { htmlFor: props.htmlFor, className: className('govuk-label', props.className) }, props.children));
export default Label;
