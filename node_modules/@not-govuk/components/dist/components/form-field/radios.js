import { createElement as h } from 'react';
import CheckboxesAndRadios from './checkboxes-and-radios';
import Hint from './hint';
import Label from './label';
import { bem } from '../../helpers';
const Radios = props => (h(CheckboxesAndRadios, { id: props.id, className: props.className, label: props.label, hint: props.hint, error: props.error, inline: props.inline, small: props.small },
    h("div", { className: bem('govuk-radios', props.small && 'small', props.inline && 'inline') }, props.options.map((v, i) => {
        const id = `${props.id}-radio-${i}`;
        return (h("div", { className: "govuk-radios__item", key: i },
            h("input", { id: id, className: "govuk-radios__input", name: props.name, type: "radio", value: v.value, checked: v.selected, disabled: props.disabled || v.disabled, onBlur: props.onBlur, onChange: props.onChange }),
            h(Label, { htmlFor: id, className: "govuk-radios__label" }, v.label),
            v.hint && h(Hint, { id: `${id}-hint`, className: "govuk-radios__hint" }, v.hint)));
    }))));
export default Radios;
