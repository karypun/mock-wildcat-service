import { createElement as h } from 'react';
import { className } from '../../helpers';
const FormGroup = props => (h("div", { id: props.id, className: className('govuk-form-group', props.className) }, props.children));
export default FormGroup;
