import { createElement as h } from 'react';
import { className } from '../../helpers';
const Hint = props => (h("span", { id: props.id, className: className('govuk-hint', props.className) }, props.children));
export default Hint;
