import * as React from 'react';
import FormField from '../form-field';
;
export const TextInput = props => React.createElement(FormField, {
    autoComplete: props.autoComplete,
    className: props.className,
    defaultValue: props.defaultValue,
    disabled: props.disabled,
    error: props.error,
    hint: props.hint,
    id: props.id,
    label: props.label,
    name: props.name,
    onBlur: props.onBlur,
    onChange: props.onChange,
    spellCheck: props.spellCheck,
    type: 'text',
    value: props.value,
    width: props.width
});
TextInput.defaultProps = {
    autoComplete: null,
    className: null,
    defaultValue: undefined,
    disabled: false,
    error: null,
    hint: null,
    id: null,
    onBlur: null,
    onChange: null,
    spellCheck: null,
    value: undefined,
    width: null
};
export default TextInput;
