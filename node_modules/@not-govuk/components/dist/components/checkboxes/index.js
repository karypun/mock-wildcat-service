import * as React from 'react';
import FormField from '../form-field';
;
;
export const Checkboxes = props => React.createElement(FormField, {
    className: props.className,
    defaultValue: props.defaultValue,
    disabled: props.disabled,
    error: props.error,
    hint: props.hint,
    id: props.id,
    inline: props.inline || false,
    label: props.label,
    multiple: true,
    name: props.name,
    onBlur: props.onBlur,
    onChange: props.onChange,
    options: props.options.map(e => ({
        disabled: e.disabled,
        hint: e.hint,
        label: e.label,
        value: e.value
    })),
    small: props.small || false,
    type: 'checkboxes',
    value: props.value
});
Checkboxes.defaultProps = {
    className: null,
    defaultValue: undefined,
    disabled: false,
    error: null,
    hint: null,
    id: null,
    inline: null,
    onBlur: null,
    onChange: null,
    small: null,
    value: undefined
};
export default Checkboxes;
