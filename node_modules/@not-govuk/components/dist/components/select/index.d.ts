import * as React from 'react';
import { SelectValue } from '../form-field';
export interface IOption {
    disabled?: boolean;
    label: string;
    selected?: boolean;
    value: string;
}
export interface ISelect {
    /** Extra CSS classes to be applied */
    className?: string;
    /** Initial value of the field */
    defaultValue?: SelectValue;
    /** Whether the field should be disabled */
    disabled?: boolean;
    /** Error message */
    error?: string;
    /** Hint */
    hint?: string;
    /** HTML id (If not specified then the name will be used) */
    id?: string;
    /** Label */
    label: any;
    /** HTML name */
    name: string;
    /** onBlur callback (for controlled fields) */
    onBlur?: (e: React.FocusEvent<any>) => void;
    /** onChange callback (for controlled fields) */
    onChange?: (e: React.ChangeEvent<any>) => void;
    /** List of options to select from */
    options: Array<IOption>;
    /** Value for controlled fields */
    value?: SelectValue;
}
export declare const Select: React.SFC<ISelect>;
export default Select;
