import * as React from 'react';
import FormField from '../form-field';
;
;
export const Select = props => React.createElement(FormField, {
    className: props.className,
    defaultValue: props.defaultValue,
    disabled: props.disabled,
    error: props.error,
    hint: props.hint,
    id: props.id,
    label: props.label,
    multiple: false,
    name: props.name,
    onBlur: props.onBlur,
    onChange: props.onChange,
    options: props.options.map(e => ({
        disabled: e.disabled,
        label: e.label,
        selected: e.selected,
        value: e.value,
    })),
    type: 'select',
    value: props.value
});
Select.defaultProps = {
    className: null,
    defaultValue: undefined,
    disabled: false,
    error: null,
    hint: null,
    id: null,
    onBlur: null,
    onChange: null,
    value: undefined
};
export default Select;
