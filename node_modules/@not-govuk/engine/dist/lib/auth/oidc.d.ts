import { AuthBagger, AuthMethod, UserProfile } from './common';
export declare type AuthOptionsOIDC = {
    method: AuthMethod.OIDC;
    issuer: string;
    clientId: string;
    clientSecret?: string;
    redirectUri: string;
    sessionsSecret: string;
};
export declare type AuthInfo = UserProfile & {
    accessToken?: string;
    refreshToken?: string;
};
export declare const oidcAuth: AuthBagger<AuthOptionsOIDC>;
export default oidcAuth;
