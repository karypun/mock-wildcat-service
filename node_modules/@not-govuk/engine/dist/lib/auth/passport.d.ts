import { Strategy } from 'passport';
import { AuthBagger } from './common';
declare type PassportOptions = {
    callback: boolean;
    id: string;
    sessions: boolean;
    sessionsSecret: string;
    strategy: Strategy;
};
export declare const passportBag: AuthBagger<PassportOptions>;
export default passportBag;
