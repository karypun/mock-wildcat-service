import { Router } from '@not-govuk/restify';
import { PageInfoSSR, PageLoader } from '@not-govuk/app-composer';
export declare const gatherPages: (pageLoader: PageLoader) => Promise<PageInfoSSR[]>;
export declare const pageRoutes: (pages: PageInfoSSR[]) => Router;
