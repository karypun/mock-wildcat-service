import { Request } from 'restify';
import { Router, Server as RestifyServer } from './router';
export declare type Server = RestifyServer & {
    apiPaths?: string[];
    forAPI?: (req: Request) => boolean;
    serveAPI?: (path: string, router: Router) => void;
};
export declare const installServeAPI: (httpd: Server) => void;
export default installServeAPI;
