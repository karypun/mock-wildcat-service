import { FC, HTMLProps, ReactNode } from 'react';
import { Breadcrumb } from '@not-govuk/breadcrumbs';
import { StandardProps } from '@not-govuk/component-helpers';
import { FooterProps, NavMenu } from '@not-govuk/footer';
import { HeaderProps } from '@not-govuk/header';
import { PhaseBannerProps } from '@not-govuk/phase-banner';
import '../assets/Page.scss';
export declare type PageProps = (StandardProps & HTMLProps<HTMLDivElement> & HeaderProps & Omit<FooterProps, 'navigation'> & Partial<PhaseBannerProps> & {
    /** Location for the Back link */
    backHref?: string;
    /** List of links */
    breadcrumbs?: Breadcrumb[];
    /** HRef for providing feedback on the service */
    feedbackHref?: string;
    /** Content for the footer */
    footerContent?: ReactNode;
    /** Content for the footer */
    footerNavigation?: NavMenu[];
    /** Content for the phase-banner */
    phaseBannerContent?: ReactNode;
    /** Title of the HTML page (can be overridden via Helmet  */
    title?: string;
});
export declare const Page: FC<PageProps>;
export default Page;
