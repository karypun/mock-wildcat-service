import { FC } from 'react';
import { StandardProps } from '@not-govuk/component-helpers';
import { LinkProps } from '@not-govuk/link';
import '../assets/Header.scss';
export declare type NavigationLink = LinkProps & {
    /** Whether the link is for the current page */
    active?: boolean;
    /** Text of the link */
    text: string;
};
export declare type HeaderProps = StandardProps & {
    /** Department branding to use (e.g. home-office) */
    department?: string;
    /** Whether to add the standard Gov.UK content */
    govUK?: boolean;
    /** Maximum width of the contents in px units (-1 for full width) */
    maxContentsWidth?: number;
    /** Navigation links */
    navigation?: NavigationLink[];
    /** Organisation link URL */
    organisationHref?: string;
    /** Organisation link text */
    organisationText?: string;
    /** Service link URL */
    serviceHref?: string;
    /** Service link text */
    serviceName?: string;
    /** Sign out link URL */
    signOutHref?: string;
    /** Sign out link text */
    signOutText?: string;
};
export declare const Header: FC<HeaderProps>;
export default Header;
