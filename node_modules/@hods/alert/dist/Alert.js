import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import { InsetText } from '@not-govuk/inset-text';
import '../assets/Alert.scss';
export const Alert = ({ children, classBlock = 'hods-alert', classModifiers, className, heading, ...attrs }) => {
    const classes = classBuilder('hods-alert', classBlock, classModifiers, className);
    return (h(InsetText, Object.assign({}, attrs, { classBlock: classBlock, classModifiers: classModifiers, className: className }),
        h("h2", { className: classes('heading') }, heading),
        h("p", null, children)));
};
export default Alert;
