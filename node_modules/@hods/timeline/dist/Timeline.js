import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/Timeline.scss';
export const Timeline = ({ children, classBlock, classModifiers, className, items, ...attrs }) => {
    const classes = classBuilder('hods-timeline', classBlock, classModifiers, className);
    return (h("div", { className: classes() }, items ? items.map(e => (h("div", { className: classes('item'), key: e.date.getTime() },
        h("h2", { className: classes('title') }, e.action),
        !e.by ? null : (h("p", { className: classes('by') },
            " by ",
            e.by)),
        h("p", { className: classes('date') },
            h("time", { dateTime: e.date.toISOString() }, e.date.toLocaleString('en-GB', { timeZone: 'UTC' }))),
        h("p", { className: classes('description') }, e.description)))) : null));
};
export default Timeline;
