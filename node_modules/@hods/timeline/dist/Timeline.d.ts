import { FC } from 'react';
import { StandardProps } from '@not-govuk/component-helpers';
import '../assets/Timeline.scss';
declare type TimelineItem = {
    /** date is used to set the datetime attribute of the <time> element so must
    be in the ISO format for a date or time. */
    action: string;
    by?: string;
    date: Date;
    description: string;
};
export declare type TimelineProps = StandardProps & {
    /** The events to be included in the timeline. */
    items: TimelineItem[];
};
export declare const Timeline: FC<TimelineProps>;
export default Timeline;
