import { createElement as h } from 'react';
import { Helmet } from 'react-helmet-async';
import { classBuilder } from '@not-govuk/component-helpers';
import { SkipLink } from '@not-govuk/components';
import { Header } from '@hods/header';
import { Footer } from '@hods/footer';
import '../assets/Page.scss';
const favicon = require('../assets/images/ho-favicon.ico').default;
const maskIcon = require('../assets/images/ho-mask-icon.svg').default;
const appleTouchIcon180 = require('../assets/images/ho-apple-touch-icon-180x180.png').default;
const appleTouchIcon167 = require('../assets/images/ho-apple-touch-icon-167x167.png').default;
const appleTouchIcon152 = require('../assets/images/ho-apple-touch-icon-152x152.png').default;
const appleTouchIcon = require('../assets/images/ho-apple-touch-icon.png').default;
const ogImage = require('../assets/images/ho-opengraph-image.png').default;
export const Page = ({ accountHref, children, classBlock, classModifiers, className, footerContent, footerNavigation, navigation, serviceHref, serviceName: _serviceName, signOutHref, signOutText, title: _title, username, ...attrs }) => {
    const classes = classBuilder('hods-page', classBlock, classModifiers, className);
    const serviceName = _serviceName || _title;
    const title = _title || _serviceName || 'Home Office';
    const headerProps = {
        accountHref,
        navigation,
        serviceHref,
        serviceName,
        signOutHref,
        signOutText,
        username
    };
    const footerProps = {
        navigation: footerNavigation
    };
    return (h("div", Object.assign({}, attrs, { className: classes() }),
        h(Helmet, null,
            h("title", null, title),
            h("meta", { name: "viewport", content: "width=device-width, initial-scale=1, viewport-fit=cover" }),
            h("meta", { httpEquiv: "X-UA-Compatible", content: "IE=edge" }),
            h("meta", { name: "theme-color", content: "#ffffff" }),
            h("link", { rel: "shortcut icon", sizes: "16x16 32x32 48x48", href: favicon, type: "image/x-icon" }),
            h("link", { rel: "mask-icon", href: maskIcon, color: "#0b0c0c" }),
            h("link", { rel: "apple-touch-icon", sizes: "180x180", href: appleTouchIcon180 }),
            h("link", { rel: "apple-touch-icon", sizes: "167x167", href: appleTouchIcon167 }),
            h("link", { rel: "apple-touch-icon", sizes: "152x152", href: appleTouchIcon152 }),
            h("link", { rel: "apple-touch-icon", href: appleTouchIcon }),
            h("meta", { property: "og:image", content: ogImage })),
        h(SkipLink, { id: "skip-link", for: "main-content" }),
        h(Header, Object.assign({}, headerProps, { className: classes('header') })),
        h("div", { className: classes('body') },
            h("div", { className: classes('container') + ' hods-width-container' },
                h("main", { id: "main-content", className: classes('main'), role: "main" }, children))),
        h(Footer, Object.assign({}, footerProps), footerContent)));
};
export default Page;
