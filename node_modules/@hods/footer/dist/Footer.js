import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import { A } from '@not-govuk/link';
import '../assets/Footer.scss';
export const Footer = ({ children = 'A Home Office Digital, Data and Technology service', classBlock, classModifiers, className, navigation, ...attrs }) => {
    const classes = classBuilder('hods-footer', classBlock, classModifiers, className);
    return (h("footer", Object.assign({}, attrs, { className: classes(), role: "contentinfo" }),
        h("div", { className: classes('inner') },
            h("div", { className: classes('container') + ' hods-width-container' },
                h("div", { className: classes('content') }, children),
                navigation && (h("div", { className: classes('navigation') },
                    h("ul", { id: "navigation", className: classes('navigation-list') }, navigation.map((v, i) => (h("li", { key: i, className: classes('navigation-item', v.active ? 'active' : undefined) },
                        h(A, { classBlock: "hods-footer__link", href: v.href, title: v.title }, v.text)))))))))));
};
export default Footer;
