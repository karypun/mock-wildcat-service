import { createElement as h } from 'react';
import { classBuilder } from '@not-govuk/component-helpers';
import '../assets/StatusBanner.scss';
export const StatusBanner = ({ children, classBlock, classModifiers, className, status, ...attrs }) => {
    const classes = classBuilder('hods-status-banner', classBlock, classModifiers, className);
    return (h("details", Object.assign({}, attrs, { className: classes() }),
        h("summary", { className: classes('summary') }, status),
        h("div", { className: classes('text') }, children)));
};
export default StatusBanner;
